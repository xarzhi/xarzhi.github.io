# 枚举

枚举也被称作 *`enums`*。枚举允许你通过列举可能的 **成员**（*variants*） 来定义一个类型。

枚举是一个很多语言都有的功能，不过不同语言中其功能各不相同。Rust 的枚举与 F#、OCaml 和 Haskell 这样的函数式编程语言中的 **代数数据类型**（*algebraic data types*）最为相似。

## 1.基本使用

### 1.1 定义枚举

我们以性别为例，性别只有男女，那么可以这样定义枚举

```rust
enum Sex{
	Man,
    
}
```

