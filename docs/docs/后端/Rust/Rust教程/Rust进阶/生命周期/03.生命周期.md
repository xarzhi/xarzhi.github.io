# 生命周期

Rust中的**生命周期**（Lifetimes）是一种泛型，它标注了**引用（reference）保持有效的作用域**。其主要目的是在编译时**防止悬垂引用**（dangling references），从而确保内存安全，且所有检查发生在编译期，没有运行时开销。

Rust的所有权系统规定：引用不能比它指向的数据活得更久。生命周期标注就是在**函数签名或结构体定义中，明确告诉编译器多个引用之间的这种存活关系**，以便编译器进行验证。



1.