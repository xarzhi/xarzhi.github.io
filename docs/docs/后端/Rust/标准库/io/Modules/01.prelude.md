# Module std::io::prelude

I/O Prelude。

该模块的目的是通过在 I/O 重型模块的顶部添加全局导入来减轻许多常见 I/O traits 的导入：

```rust
use std::io::prelude::*;
```



## 再导出

```rust
pub use super::BufRead;
pub use super::Read;
pub use super::Seek;
pub use super::Write;
```

